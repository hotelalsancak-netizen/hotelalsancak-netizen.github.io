# --- şifreler / kimlik (ASLA commit) ---
.env
gizli.py
ayarlar.ini
auth_state.json

# --- MİSAFİR VERİSİ (public repo — ASLA commit) ---
cards.json
cards*.json
occupancy.json
room_changes.json
findings.json
report.html
report.pdf
*.zip
cardreads/
testdata/
discover/
raporlar/
reports/
# not: site_data/cardreads-incoming/*.zip.enc ŞİFRELİ → *.zip ile eşleşmez, commit edilir.

# --- kart işleme scriptleri: PII yorumları temizlendi → repoda (ofis git ile çeker) ---
#     analyze.py, build_report.py, parse_cards.py, kart_yukle.py commit EDİLİR.
# --- hâlâ yalnızca yerel (yorumlarında PII olabilir, taranmadı) ---
room_changes.py
package_report.py
discover.py
elektra.py
gunsonu_app.py
publish_web.py

# --- üretilen çıktı / geçici ---
public/
__pycache__/
build/
dist/
*.spec
.venv/
hata.log

# not: site_data/ (şifreli kart bloğu) commit EDİLİR — güvenli.
parite.py
